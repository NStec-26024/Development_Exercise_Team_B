<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.fullness.stationary.repository.ProductRepository">

    <resultMap id="productResultMap" type="com.example.fullness.stationary.entity.Product">
        <id property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="price" column="price"/>
        <result property="categoryId" column="product_category_id"/>
        <result property="imageUrl" column="image_url"/>

        <!--  ProductCategory にマッピング -->
        <association property="productCategory" javaType="com.example.fullness.stationary.entity.ProductCategory">
            <id property="id" column="product_category_id"/>
            <result property="name" column="category_name"/>
        </association>

        <!--  ProductStock にマッピング -->
        <association property="productStock" javaType="com.example.fullness.stationary.entity.ProductStock">
            <id property="id" column="stock_id"/>
            <result property="productId" column="stock_product_id"/>
            <result property="quantity" column="stock_quantity"/>
        </association>
    </resultMap>

    <!-- 全商品件数（削除済み除く） -->
    <select id="countAll" resultType="int">
        <![CDATA[
            SELECT COUNT(*) 
            FROM product 
            WHERE delete_flg = 0
        ]]>
    </select>

    <!-- カテゴリ別商品件数（削除済み除く） -->
    <select id="countByCategory" resultType="int">
        <![CDATA[
            SELECT COUNT(*)
            FROM product
            WHERE product_category_id = #{categoryId}
            AND delete_flg = 0
        ]]>
    </select>

    <!-- 全商品ページング取得（カテゴリ名・在庫含む） -->
    <select id="selectAllWithPaging" resultMap="productResultMap">
        <![CDATA[
            SELECT
                p.id,
                p.name,
                p.price,
                p.product_category_id,
                p.image_url,
                pc.name AS category_name,
                ps.id AS stock_id,
                ps.product_id AS stock_product_id,
                ps.quantity AS stock_quantity
            FROM product p
            LEFT JOIN product_category pc ON p.product_category_id = pc.id
            LEFT JOIN product_stock ps ON p.id = ps.product_id
            WHERE p.delete_flg = 0
            ORDER BY p.id
            LIMIT #{limit} OFFSET #{offset}
            ]]>
    </select>

    <!-- カテゴリ別ページング取得（カテゴリ名・在庫含む） -->
    <select id="selectByCategoryWithPaging" resultMap="productResultMap">
        <![CDATA[
            SELECT
                p.id,
                p.name,
                p.price,
                p.product_category_id,
                p.image_url,
                pc.name AS category_name,
                ps.id AS stock_id,
                ps.product_id AS stock_product_id,
                ps.quantity AS stock_quantity
            FROM product p
            LEFT JOIN product_category pc ON p.product_category_id = pc.id
            LEFT JOIN product_stock ps ON p.id = ps.product_id
            WHERE p.product_category_id = #{categoryId}
            AND p.delete_flg = 0
            ORDER BY p.id
            LIMIT #{limit} OFFSET <div id="">offset</div>
        ]]>
    </select>

    <!-- 商品 ID 取得（削除済み除く） -->
    <select id="selectById" resultMap="productResultMap">
        <![CDATA[
            SELECT
                p.id,
                p.name,
                p.price,
                p.product_category_id,
                p.image_url,
                pc.name AS category_name,
                ps.id AS stock_id,
                ps.product_id AS stock_product_id,
                ps.quantity AS stock_quantity
            FROM product p
            LEFT JOIN product_category pc ON p.product_category_id = pc.id
            LEFT JOIN product_stock ps ON p.id = ps.product_id
            WHERE p.id = #{id}
            AND p.delete_flg = 0
        ]]>
    </select>

    <!-- 論理削除 -->
    <update id="deleteById">
        <![CDATA[
            UPDATE product
            SET delete_flg = 1
            WHERE id = #{id}
        ]]>
    </update>
    
    <!-- 商品情報更新（商品名・単価・カテゴリ・画像URL） -->
    <update id="update" parameterType="com.example.fullness.stationary.entity.Product">
        <![CDATA[
            UPDATE product
            SET
            name = #{name},
            price = #{price},
            product_category_id = #{categoryId},
            image_url = #{imageUrl}
            WHERE id = #{id}
            AND delete_flg = 0
        ]]>
    </update>

    <insert id="insertProduct" useGeneratedKeys="true" keyProperty="id">
        <![CDATA[
            INSERT INTO product (
            product_category_id,
            name,
            price,
            image_url) 

            VALUES (
            #{categoryId},
            #{name},
            #{price},
            #{imageUrl})
        ]]>
    </insert>



</mapper>

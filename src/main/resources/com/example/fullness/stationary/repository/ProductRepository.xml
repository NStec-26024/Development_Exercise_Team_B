<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.fullness.stationary.repository.ProductRepository">

    <!-- 全商品件数を取得 -->
    <select id="countAll" resultType="int">
         <![CDATA[
            SELECT COUNT(*) FROM product
        ]]>
    </select>

    <!-- ★ 全商品をページングで取得（カテゴリ名も取得） -->
    <select id="findAllWithPaging" resultType="com.example.fullness.stationary.entity.Product">
         <![CDATA[
            SELECT 
                p.id,
                p.name,
                p.price,
                p.product_category_id,
                p.image_url,
                pc.name AS category_name
            FROM 
                product p
            LEFT JOIN 
                product_category pc ON p.product_category_id = pc.id
            ORDER BY 
                p.id
            LIMIT #{limit} OFFSET #{offset}
            ]]>
    </select>

    <!-- カテゴリ別商品件数を取得 -->
    <select id="countByCategory" resultType="int">
         <![CDATA[
          SELECT 
            COUNT(*) 
            FROM 
                product
            WHERE 
            product_category_id = #{categoryId}
    ]]>
    </select>

    <!-- ★ カテゴリ別商品をページングで取得（カテゴリ名も取得） -->
    <select id="findByCategoryWithPaging" resultType="com.example.fullness.stationary.entity.Product">
         <![CDATA[
            SELECT 
                p.id,
                p.name,
                p.price,
                p.product_category_id,
                p.image_url,
                pc.name AS category_name
            FROM 
                product p
            LEFT JOIN 
                product_category pc ON p.product_category_id = pc.id
            WHERE 
                p.product_category_id = #{categoryId}
            ORDER BY 
                p.id
            LIMIT #{limit} OFFSET #{offset}
            ]]>
    </select>

    <!-- IDで商品を取得 -->
    <select id="findById" resultType="com.example.fullness.stationary.entity.Product">
         <![CDATA[
            SELECT 
                p.id,
                p.name,
                p.price,
                p.product_category_id,
                p.image_url,
                pc.name AS category_name
            FROM 
                product p
            LEFT JOIN 
                product_category pc ON p.product_category_id = pc.id
            WHERE 
                p.id = #{id}
        ]]>
    </select>

    <!-- 商品論理削除 -->
    <update id="deleteById">
         <![CDATA[
            UPDATE product
            SET delete_flg = 1
            WHERE id = #{id}
        ]]>
    </update>

</mapper>